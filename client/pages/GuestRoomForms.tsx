import { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { Search, UserRound } from "lucide-react";
import { loadCustomers, upsertCustomer, type Customer } from "../lib/customerStore";

type GuestRoom = { id: string; type: string; beds: string; guests: number };
export type BookingGuest = { name: string; phone: string; identityNumber: string };

export default function GuestRoomForms({ rooms, guest, onGuestChange }: { rooms: GuestRoom[]; guest: BookingGuest; onGuestChange: (guest: BookingGuest) => void }) {
  const { t } = useTranslation();
  const [customerQuery, setCustomerQuery] = useState("");
  const [customers, setCustomers] = useState<Customer[]>(loadCustomers);
  useEffect(() => { if (guest.name.trim() && guest.phone.trim()) { const next = upsertCustomer(guest.name, guest.phone, guest.identityNumber); if (next) setCustomers(next); } }, [guest]);
  const search = customerQuery.trim().toLowerCase();
  const matches = search ? customers.filter((customer) => `${customer.name} ${customer.phone} ${customer.email} ${customer.identityNumber}`.toLowerCase().includes(search)).slice(0, 5) : [];
  const updateGuest = (field: keyof BookingGuest, value: string) => onGuestChange({ ...guest, [field]: value });
  const chooseCustomer = (customer: Customer) => { onGuestChange({ name: customer.name, phone: customer.phone, identityNumber: customer.identityNumber }); setCustomerQuery(""); };
  return <div className="space-y-4"><div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="mb-4 flex items-center gap-3 border-b border-slate-100 pb-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-violet-100 text-xs font-bold text-violet-700"><UserRound size={16} /></div><div><p className="text-sm font-bold text-slate-900">{t("booking.stayingGuest", "Guest staying")}</p><p className="mt-0.5 text-xs text-slate-500">{rooms.length} {t("booking.rooms")}</p></div></div><div className="relative mb-4"><label className="text-sm font-semibold text-slate-700">{t("booking.searchCustomer", "Tìm khách hàng đã lưu")}</label><div className="relative mt-1.5"><Search size={15} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input value={customerQuery} onChange={(event) => setCustomerQuery(event.target.value)} placeholder={t("booking.searchCustomerPlaceholder", "Tìm theo tên hoặc số điện thoại")} className="h-10 w-full rounded-lg border border-slate-200 pl-9 pr-3 text-sm outline-none focus:border-violet-400 focus:ring-2 focus:ring-violet-100" /></div>{matches.length > 0 && <div className="absolute z-10 mt-1 w-full overflow-hidden rounded-lg border border-slate-200 bg-white shadow-lg">{matches.map((customer) => <button type="button" key={customer.id} onClick={() => chooseCustomer(customer)} className="flex w-full items-center justify-between px-3 py-2 text-left text-sm hover:bg-violet-50"><span className="font-semibold text-slate-700">{customer.name}</span><span className="text-xs text-slate-400">{customer.phone}</span></button>)}</div>}</div><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-semibold text-slate-700">{t("customer.fullName")}<input value={guest.name} onChange={(event) => updateGuest("name", event.target.value)} placeholder={t("common.guestNamePlaceholder", "Nguyễn Văn A")} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-400 focus:ring-2 focus:ring-violet-100" /></label><label className="text-sm font-semibold text-slate-700">{t("customer.phone")}<input value={guest.phone} onChange={(event) => updateGuest("phone", event.target.value)} placeholder="090 000 0000" className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-400 focus:ring-2 focus:ring-violet-100" /></label><label className="text-sm font-semibold text-slate-700">{t("customer.identityNumber")}<input value={guest.identityNumber} onChange={(event) => updateGuest("identityNumber", event.target.value)} placeholder="001234567890" className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-400 focus:ring-2 focus:ring-violet-100" /></label></div></div>{rooms.map((room, index) => <div key={room.id} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="mb-4 flex items-center gap-3 border-b border-slate-100 pb-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-violet-100 text-xs font-bold text-violet-700"><UserRound size={16} /></div><div><p className="text-sm font-bold text-slate-900">{t("booking.stayingGuest", "Guest staying in room")} {room.id}</p><p className="mt-0.5 text-xs text-slate-500">{room.type} · {room.beds} · {t("room.maxGuestsLabel", "Up to {{count}} guests", { count: room.guests })}</p></div><span className="ml-auto rounded-full bg-slate-100 px-2 py-1 text-[10px] font-semibold text-slate-500">{t("booking.guestNumber", "Guest {{count}}", { count: index + 1 })}</span></div><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-semibold text-slate-700">{t("booking.adults")}<select defaultValue="2" className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm font-normal"><option>1</option><option>2</option><option>3</option><option>4</option></select></label><label className="text-sm font-semibold text-slate-700">{t("booking.children")}<select defaultValue="0" className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm font-normal"><option>0</option><option>1</option><option>2</option></select></label><label className="text-sm font-semibold text-slate-700">{t("booking.infants")}<select defaultValue="0" className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm font-normal"><option>0</option><option>1</option><option>2</option></select></label></div><label className="mt-4 block text-sm font-semibold text-slate-700">{t("booking.privateNote", "Private note for room")} {room.id}<textarea rows={2} placeholder={t("booking.specialRequest", "Special guest requests...")} className="mt-1.5 w-full resize-none rounded-lg border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-400" /></label></div>)}</div>;
}

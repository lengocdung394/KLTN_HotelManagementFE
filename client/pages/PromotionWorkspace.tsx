import { useState } from "react";
import { useTranslation } from "react-i18next";
import { CalendarDays, Copy, Plus, Tag, ToggleLeft, ToggleRight } from "lucide-react";

const initialPromotions = [
  { id: 1, name: "Ưu đãi cuối tuần", code: "WEEKEND15", detail: "Giảm 15% cho đặt phòng từ thứ Sáu đến Chủ nhật", value: "15%", period: "01/10/2024 – 31/12/2024", active: true, tone: "bg-blue-50 text-blue-700" },
  { id: 2, name: "Khách hàng thân thiết", code: "LOYAL10", detail: "Ưu đãi dành cho khách quay lại lưu trú", value: "10%", period: "01/10/2024 – 31/03/2025", active: true, tone: "bg-emerald-50 text-emerald-700" },
  { id: 3, name: "Mùa thấp điểm", code: "LOWSEASON", detail: "Giảm giá phòng Suite trong mùa thấp điểm", value: "20%", period: "01/11/2024 – 28/02/2025", active: false, tone: "bg-amber-50 text-amber-700" },
];

export default function PromotionWorkspace() {
  const { t } = useTranslation();
  const [promotions, setPromotions] = useState(initialPromotions);
  const [copied, setCopied] = useState<string | null>(null);
  const [modalOpen, setModalOpen] = useState(false);
  const [form, setForm] = useState({ name: "", code: "", value: "10%", detail: "" });
  const [range, setRange] = useState({ start: "2025-01-01", end: "2025-03-31" });
  const toggle = (id: number) => setPromotions((current) => current.map((promotion) => promotion.id === id ? { ...promotion, active: !promotion.active } : promotion));
  const copyCode = (code: string) => { navigator.clipboard?.writeText(code); setCopied(code); window.setTimeout(() => setCopied(null), 1500); };
  const label = (key: string, options?: Record<string, string>) => t(`promotion.${key}`, options);
  const addPromotion = (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    setPromotions((current) => [...current, { id: Date.now(), name: form.name || label("newPromotion"), code: form.code.toUpperCase() || "NEW10", detail: form.detail || label("defaultDetail"), value: form.value, period: `${range.start} – ${range.end}`, active: true, tone: "bg-blue-50 text-blue-700" }]);
    setForm({ name: "", code: "", value: "10%", detail: "" });
    setModalOpen(false);
  };
  return <div className="mt-6 space-y-5">
    {modalOpen && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/30 p-4"><form onSubmit={addPromotion} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><h3 className="text-lg font-bold text-slate-900">{label("createTitle")}</h3><p className="mt-1 text-sm text-slate-500">{label("createDescription")}</p></div><button type="button" onClick={() => setModalOpen(false)} aria-label={t("common.close")} className="text-2xl leading-none text-slate-400">×</button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-700 sm:col-span-2">{label("programName")}<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder={label("exampleWeekendOffer")} className="mt-1.5 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm font-normal" /></label><label className="text-sm font-semibold text-slate-700">{label("code")}<input required value={form.code} onChange={(event) => setForm({ ...form, code: event.target.value.toUpperCase() })} placeholder="WEEKEND20" className="mt-1.5 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm font-normal" /></label><label className="text-sm font-semibold text-slate-700">{label("discount")}<select value={form.value} onChange={(event) => setForm({ ...form, value: event.target.value })} className="mt-1.5 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal">{["10%", "15%", "20%", "25%"].map((value) => <option key={value}>{value}</option>)}</select></label><div className="sm:col-span-2"><p className="text-sm font-semibold text-slate-700">{label("validity")}</p><div className="mt-1.5 grid gap-3 sm:grid-cols-2"><label className="text-xs font-medium text-slate-500">{label("fromDate")}<input required type="date" value={range.start} onChange={(event) => setRange({ ...range, start: event.target.value })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal" /></label><label className="text-xs font-medium text-slate-500">{label("toDate")}<input required type="date" min={range.start} value={range.end} onChange={(event) => setRange({ ...range, end: event.target.value })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm font-normal" /></label></div></div><label className="text-sm font-semibold text-slate-700 sm:col-span-2">{label("description")}<textarea value={form.detail} onChange={(event) => setForm({ ...form, detail: event.target.value })} placeholder={label("descriptionPlaceholder")} className="mt-1.5 min-h-20 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm font-normal" /></label></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={() => setModalOpen(false)} className="rounded-lg px-4 py-2.5 text-sm font-semibold text-slate-600">{label("cancel")}</button><button type="submit" className="rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white">{label("save")}</button></div></form></div>}
    <section className="rounded-2xl border border-slate-200/80 bg-white shadow-sm"><div className="flex flex-col gap-4 border-b border-slate-100 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><Tag size={20} /></div><div><h3 className="font-bold text-slate-900">{label("programs")}</h3><p className="mt-1 text-sm text-slate-500">{label("programsDescription")}</p></div></div><button onClick={() => setModalOpen(true)} className="flex w-fit items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white"><Plus size={16} />{label("create")}</button></div><div className="grid gap-4 p-5 sm:grid-cols-3"><div className="rounded-xl bg-blue-50 p-4"><p className="text-xs font-semibold text-blue-700">{label("active")}</p><p className="mt-2 text-2xl font-bold text-blue-900">{promotions.filter((promotion) => promotion.active).length}</p></div><div className="rounded-xl bg-emerald-50 p-4"><p className="text-xs font-semibold text-emerald-700">{label("usesThisMonth")}</p><p className="mt-2 text-2xl font-bold text-emerald-900">128</p></div><div className="rounded-xl bg-amber-50 p-4"><p className="text-xs font-semibold text-amber-700">{label("guestSavings")}</p><p className="mt-2 text-2xl font-bold text-amber-900">18,6tr</p></div></div></section>
    <section className="rounded-2xl border border-slate-200/80 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h3 className="font-bold text-slate-900">{label("codeList")}</h3><p className="mt-1 text-sm text-slate-500">{label("codeListDescription")}</p></div><div className="divide-y divide-slate-100">{promotions.map((promotion) => <article key={promotion.id} className="flex flex-col gap-4 p-5 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-start gap-3"><div className={`grid h-10 w-10 place-items-center rounded-xl ${promotion.tone}`}><Tag size={18} /></div><div><div className="flex flex-wrap items-center gap-2"><h4 className="font-bold text-slate-900">{promotion.name}</h4><button onClick={() => copyCode(promotion.code)} title={label("copyCode")} className="flex items-center gap-1 rounded-md bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-600"><Copy size={11} />{copied === promotion.code ? label("copied") : promotion.code}</button></div><p className="mt-1 text-sm text-slate-500">{promotion.detail}</p><p className="mt-2 flex items-center gap-1.5 text-xs text-slate-400"><CalendarDays size={13} />{promotion.period}</p></div></div><div className="flex items-center justify-between gap-5 lg:justify-end"><div><p className="text-xs text-slate-400">{label("offerLevel")}</p><p className="mt-1 text-xl font-bold text-slate-900">{promotion.value}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${promotion.active ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-500"}`}>{promotion.active ? label("active") : label("paused")}</span><button onClick={() => toggle(promotion.id)} aria-label={label("toggleStatus", { name: promotion.name })} className="text-blue-600">{promotion.active ? <ToggleRight size={28} /> : <ToggleLeft size={28} />}</button></div></article>)}</div></section>
  </div>;
}
